const numberOfStudents = 15;
const numberOfMentors = 8;
const total=numberOfStudents+numberOfMentors;


const totalStudents=`Percentage students: ${Math.round(numberOfStudents*100/23)}%`;
const totalMentors=`Percentage mentors: ${Math.round(numberOfMentors*100/23)}%`;

console.log(totalStudents);
console.log(totalMentors);

